# CS50 C Practice

Small C programs written while learning CS50x.

## Programs

- `hello.c` — prints hello world
- `compare.c` — compares two integers
- `password.c` — simple password check using `strcmp`
- `password_attempts.c` — password check with 3 attempts
